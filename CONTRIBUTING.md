# Contributing to CoalOmit

We welcome contributions!

## Guidelines
1. Fork the repo and create feature branches from `main`.
2. Write unit tests under `packages/comit-core/tests` and `packages/comit-cli/tests`.
3. Run `pytest` to ensure all tests pass before submitting a PR.
4. Follow PEP8 Python style guidelines with type annotations.
